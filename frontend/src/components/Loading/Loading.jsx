// Importing necessary components from React
import React from 'react';

// Importing styles
import './Loading.css';

// Loading component represents a loading state while the application is rendering
const Loading = () => {
  return (
    <p className='loading-text'>Loading the site, please wait a moment...</p>
  );
};

// Exporting the Loading component
export default Loading;